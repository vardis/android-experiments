<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.giorgos.fragments.app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.giorgos.fragments.app.CarListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.giorgos.fragments.app.CarDetailActivity"
            android:label="@string/title_car_detail"
            android:parentActivityName="com.giorgos.fragments.app.CarListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.giorgos.fragments.app.CarListActivity" />
        </activity>
    </application>

</manifest>
